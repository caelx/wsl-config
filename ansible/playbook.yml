---
- name: Configure WSL2 Arch Linux Environment
  hosts: localhost
  connection: local
  gather_facts: true

  roles:
    - role: wsl
    - role: packages
    - role: fish
    - role: ssh
    - role: antigravity-bridge
